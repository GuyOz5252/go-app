# Empty = same origin as the app (use Vite dev proxy to the Go API).
PUBLIC_API_BASE=

# Target for Vite dev server proxy (/api and WebSocket). Browser cannot send Authorization on WS;
# the client appends ?access_token= to the WebSocket URL and the proxy rewrites it to Authorization.
PUBLIC_DEV_PROXY_TARGET=http://localhost:8080
